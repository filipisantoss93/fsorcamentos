let perfilGeradorAtual=null;let usuarioGeradorId=null;let orcamentoSalvoAtualId=null;let linkOrcamentoAtual='';
const FS_REC_MAO='fs_mao_obra_recorrente_v1';const FS_REC_PROD='fs_itens_recorrentes_v1';
function $(id){return document.getElementById(id)}function moeda(v){return Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}function html(v){return String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}function numero(v){return Number(String(v||'').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,''))||0}function planoNorm(v){const p=String(v||'gratis').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').trim();return ['premium','basico','gestao'].includes(p)?'premium':'gratis'}function ehPremium(){return planoNorm(localStorage.getItem('usuario_plano')||perfilGeradorAtual?.plano)==='premium'}
function abrirModalLoginGerador(){const m=$('modal-login');if(m){m.style.display='flex';setTimeout(()=>$('auth-email')?.focus(),150)}}function fecharModalLoginGerador(){const m=$('modal-login');if(m)m.style.display='none'}function abrirModalLogin(){abrirModalLoginGerador()}function fecharModalLogin(){fecharModalLoginGerador()}
async function iniciarGerador(){try{if(!window._supabase&&window.supabase&&window.SUPABASE_URL&&window.SUPABASE_ANON_KEY)window._supabase=window.supabase.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);const{data:{session}}=await _supabase.auth.getSession();if(!session?.user?.id){abrirModalLoginGerador();return}usuarioGeradorId=session.user.id;await carregarPerfilGerador(session.user.id);$('conteudo-gerador').style.display='grid';prepararDatas();adicionarLinhaOrcamento('mao_obra');adicionarLinhaOrcamento('produto');renderRecorrentes();aplicarPlanoGerador();restaurarRascunho();gerarPreviaOrcamento()}catch(e){console.error(e);mostrarStatus('Erro ao carregar o gerador. Atualize a página.')}}
async function carregarPerfilGerador(userId){try{const{data}=await _supabase.from('perfis').select('*').eq('id',userId).maybeSingle();perfilGeradorAtual=data||{};if(data?.plano)localStorage.setItem('usuario_plano',planoNorm(data.plano))}catch(e){console.warn('Perfil não carregado:',e);perfilGeradorAtual={}}}
function prepararDatas(){if(!$('validade')?.value){const d=new Date();d.setDate(d.getDate()+7);$('validade').value=d.toISOString().slice(0,10)}}
function mostrarStatus(t){const el=$('status-sessao-card');if(!el)return;el.style.display=t?'block':'none';el.textContent=t||''}
function aplicarPlanoGerador(){const premium=ehPremium();$('btn-plano-gerador').textContent=premium?'Plano Premium ativo':'Plano Grátis';$('btn-plano-gerador').href=premium?'/orcamentos.html':'/planos.html#assinar-plano-premium';['btn-salvar-premium','btn-whatsapp-premium'].forEach(id=>{const b=$(id);if(b)b.disabled=!premium});document.querySelectorAll('.fs-anuncio-gratis').forEach(e=>e.style.display=premium?'none':'block');if($('texto-premium-gerador'))$('texto-premium-gerador').textContent=premium?'Premium ativo: salve, envie link e acompanhe aprovação online.':'No Grátis você gera PDF. Assine o Premium para salvar e enviar link.';try{if(!premium)(adsbygoogle=window.adsbygoogle||[]).push({})}catch(_){}}
function criarLinha(tipo,item={}){const id='row_'+Math.random().toString(36).slice(2);const wrap=document.createElement('div');wrap.className='linha';wrap.dataset.tipo=tipo;wrap.id=id;wrap.innerHTML=`<div><small>Descrição</small><input class="desc" value="${html(item.descricao||'')}" placeholder="${tipo==='mao_obra'?'Ex: Troca de pastilhas':'Ex: Jogo de pastilhas'}"></div><div><small>Qtd</small><input class="qtd" type="number" min="0" step="0.01" value="${html(item.qtd??1)}"></div><div><small>Unitário</small><input class="valor" type="number" min="0" step="0.01" value="${html(item.valor??0)}"></div><div><small>Subtotal</small><strong class="subtotal">R$ 0,00</strong></div><button type="button" class="btn red" onclick="removerLinhaOrcamento('${id}')">×</button>`;wrap.querySelectorAll('input').forEach(i=>i.addEventListener('input',()=>{calcularTotal();salvarRascunho()}));return wrap}
function adicionarLinhaOrcamento(tipo,item={}){const lista=tipo==='mao_obra'?'lista-mao-obra':'lista-produtos';$(lista)?.appendChild(criarLinha(tipo,item));calcularTotal();salvarRascunho()}function removerLinhaOrcamento(id){const el=$(id);const tipo=el?.dataset.tipo;el?.remove();if(tipo&&document.querySelectorAll(`.linha[data-tipo="${tipo}"]`).length===0)adicionarLinhaOrcamento(tipo);calcularTotal();salvarRascunho()}
function coletarItens(tipo=null){return Array.from(document.querySelectorAll('.linha')).map(row=>{const qtd=numero(row.querySelector('.qtd')?.value||1),valor=numero(row.querySelector('.valor')?.value||0),descricao=row.querySelector('.desc')?.value?.trim()||'';return{tipo:row.dataset.tipo,descricao,qtd,quantidade:qtd,valor,valor_unitario:valor,subtotal:qtd*valor,total:qtd*valor}}).filter(i=>i.descricao&&i.qtd>0&&(!tipo||i.tipo===tipo))}
function calcularTotal(){let total=0;document.querySelectorAll('.linha').forEach(row=>{const sub=numero(row.querySelector('.qtd')?.value||0)*numero(row.querySelector('.valor')?.value||0);total+=sub;const s=row.querySelector('.subtotal');if(s)s.textContent=moeda(sub)});$('total-orcamento').textContent='Total: '+moeda(total);return total}
function dadosOrcamento(){return{titulo:$('titulo')?.value?.trim()||'Orçamento',cliente:$('cliente')?.value?.trim()||'Cliente',telefone:$('tel-cliente')?.value?.trim()||'',validade:$('validade')?.value||'',observacoes:$('observacoes')?.value?.trim()||'',maoObra:coletarItens('mao_obra'),produtos:coletarItens('produto'),total:calcularTotal()}}
function gerarHtmlOrcamento(){const d=dadosOrcamento();const empresa=perfilGeradorAtual?.nome_empresa||'FS Orçamentos';const tel=perfilGeradorAtual?.telefone_empresa||'';const resp=perfilGeradorAtual?.nome||'';const hoje=new Date().toLocaleDateString('pt-BR');const tabela=(titulo,itens)=>`<h3 style="margin:18px 0 6px;color:#0f172a">${titulo}</h3><table style="width:100%;border-collapse:collapse;font-size:12px"><thead><tr style="background:#0f172a;color:#fff"><th style="padding:8px;text-align:left">Descrição</th><th style="padding:8px;text-align:right">Qtd</th><th style="padding:8px;text-align:right">Unit.</th><th style="padding:8px;text-align:right">Subtotal</th></tr></thead><tbody>${itens.length?itens.map(i=>`<tr><td style="padding:8px;border-bottom:1px solid #e5e7eb">${html(i.descricao)}</td><td style="padding:8px;border-bottom:1px solid #e5e7eb;text-align:right">${i.qtd}</td><td style="padding:8px;border-bottom:1px solid #e5e7eb;text-align:right">${moeda(i.valor)}</td><td style="padding:8px;border-bottom:1px solid #e5e7eb;text-align:right">${moeda(i.subtotal)}</td></tr>`).join(''):`<tr><td colspan="4" style="padding:8px;border-bottom:1px solid #e5e7eb">Nenhum item informado.</td></tr>`}</tbody></table>`;return`<div style="width:794px;min-height:1123px;box-sizing:border-box;padding:34px;font-family:Arial,Helvetica,sans-serif;color:#111;background:#fff"><div style="display:flex;justify-content:space-between;gap:20px;border-bottom:4px solid #0f172a;padding-bottom:14px"><div><h1 style="margin:0;color:#0f172a;font-size:28px">${html(empresa)}</h1><p style="margin:5px 0 0;color:#475569">${html(tel)}${resp?' • '+html(resp):''}</p></div><div style="text-align:right"><strong style="font-size:22px;color:#0f172a">ORÇAMENTO</strong><p style="margin:5px 0;color:#475569">${hoje}</p></div></div><div style="margin-top:18px;display:grid;grid-template-columns:1fr 1fr;gap:10px"><div style="background:#f8fafc;border:1px solid #e5e7eb;border-radius:8px;padding:10px"><small>Cliente</small><strong style="display:block">${html(d.cliente)}</strong>${d.telefone?`<span>${html(d.telefone)}</span>`:''}</div><div style="background:#f8fafc;border:1px solid #e5e7eb;border-radius:8px;padding:10px"><small>Título</small><strong style="display:block">${html(d.titulo)}</strong>${d.validade?`<span>Validade: ${new Date(d.validade+'T00:00:00').toLocaleDateString('pt-BR')}</span>`:''}</div></div>${tabela('Mão de obra',d.maoObra)}${tabela('Itens / produtos',d.produtos)}<div style="margin-top:18px;text-align:right;font-size:26px;font-weight:900;color:#0f172a">Total: ${moeda(d.total)}</div>${d.observacoes?`<div style="margin-top:18px;border:1px solid #e5e7eb;border-radius:8px;padding:10px"><strong>Observações</strong><p style="white-space:pre-wrap;margin:6px 0 0">${html(d.observacoes)}</p></div>`:''}</div>`}
function gerarPreviaOrcamento(){if(!coletarItens().length)return alert('Adicione pelo menos uma mão de obra ou produto.');$('conteudo-pdf').innerHTML=gerarHtmlOrcamento();$('area-previa').style.display='block';calcularTotal();salvarRascunho()}
function baixarPDF(){gerarPreviaOrcamento();document.body.classList.add('gerando-pdf');const nome=($('titulo')?.value||'orcamento').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'orcamento';html2pdf().set({margin:0,filename:`${nome}.pdf`,image:{type:'jpeg',quality:.98},html2canvas:{scale:2,useCORS:true},jsPDF:{unit:'px',format:[794,1123],orientation:'portrait'}}).from($('conteudo-pdf')).save().finally(()=>document.body.classList.remove('gerando-pdf'))}
async function salvarOrcamentoPremium(){if(!ehPremium())return location.href='/planos.html#assinar-plano-premium';gerarPreviaOrcamento();const d=dadosOrcamento();const itens=[...d.maoObra,...d.produtos];try{const payload={usuario_id:usuarioGeradorId,cliente_nome:d.cliente,cliente_whatsapp:d.telefone,titulo:d.titulo,total:d.total,status:'pendente',itens,observacoes:d.observacoes,criado_em:new Date().toISOString()};let resp;if(orcamentoSalvoAtualId){resp=await _supabase.from('orcamentos').update(payload).eq('id',orcamentoSalvoAtualId).eq('usuario_id',usuarioGeradorId).select('id').maybeSingle()}else{resp=await _supabase.from('orcamentos').insert(payload).select('id').single()}if(resp.error)throw resp.error;orcamentoSalvoAtualId=resp.data?.id||orcamentoSalvoAtualId;linkOrcamentoAtual=`${location.origin}/aprovacao.html?id=${encodeURIComponent(orcamentoSalvoAtualId)}`;try{await _supabase.from('orcamentos').update({link_publico:linkOrcamentoAtual}).eq('id',orcamentoSalvoAtualId).eq('usuario_id',usuarioGeradorId)}catch(_){}mostrarStatus('Orçamento salvo. Link: '+linkOrcamentoAtual);return orcamentoSalvoAtualId}catch(e){console.error(e);alert('Não foi possível salvar. Verifique a tabela orcamentos no Supabase.')}}
async function enviarWhatsappPremium(){if(!ehPremium())return location.href='/planos.html#assinar-plano-premium';const id=orcamentoSalvoAtualId||await salvarOrcamentoPremium();if(!id)return;const d=dadosOrcamento();const tel=d.telefone.replace(/\D/g,'');if(!tel)return alert('Informe o WhatsApp do cliente.');const numero=tel.startsWith('55')?tel:'55'+tel;const msg=`Olá ${d.cliente}, segue seu orçamento: ${linkOrcamentoAtual}\n\nVocê pode aprovar ou recusar pelo link.`;window.open(`https://wa.me/${numero}?text=${encodeURIComponent(msg)}`,'_blank')}
function carregarLista(chave){try{return JSON.parse(localStorage.getItem(chave)||'[]')}catch(_){return[]}}function salvarLista(chave,lista){localStorage.setItem(chave,JSON.stringify(lista.slice(0,80)))}function salvarRecorrentes(tipo){if(!ehPremium())return location.href='/planos.html#assinar-plano-premium';const itens=coletarItens(tipo);if(!itens.length)return alert('Preencha pelo menos um item para salvar como recorrente.');const chave=tipo==='mao_obra'?FS_REC_MAO:FS_REC_PROD;const lista=carregarLista(chave);itens.forEach(i=>{if(!lista.some(x=>x.descricao===i.descricao&&Number(x.valor)===Number(i.valor)))lista.unshift({descricao:i.descricao,qtd:i.qtd,valor:i.valor})});salvarLista(chave,lista);renderRecorrentes();alert('Recorrente salvo.')}
function usarRecorrente(tipo,index){const chave=tipo==='mao_obra'?FS_REC_MAO:FS_REC_PROD;const item=carregarLista(chave)[index];if(item)adicionarLinhaOrcamento(tipo,item)}function excluirRecorrente(tipo,index){const chave=tipo==='mao_obra'?FS_REC_MAO:FS_REC_PROD;const lista=carregarLista(chave);lista.splice(index,1);salvarLista(chave,lista);renderRecorrentes()}function renderRecorrentes(){const render=(id,tipo,chave)=>{const box=$(id);if(!box)return;const lista=carregarLista(chave);box.innerHTML=lista.length?lista.map((i,idx)=>`<div class="rec-item"><span>${html(i.descricao)} • ${moeda(i.valor)}</span><div><button class="btn" type="button" onclick="usarRecorrente('${tipo}',${idx})">Usar</button><button class="btn red" type="button" onclick="excluirRecorrente('${tipo}',${idx})">×</button></div></div>`).join(''):'<small>Nenhum recorrente salvo.</small>'};render('rec-mao-obra','mao_obra',FS_REC_MAO);render('rec-produtos','produto',FS_REC_PROD)}
function salvarRascunho(){try{localStorage.setItem('fs_rascunho_orcamento',JSON.stringify(dadosOrcamento()))}catch(_){}}function restaurarRascunho(){try{const r=JSON.parse(localStorage.getItem('fs_rascunho_orcamento')||'null');if(!r)return;$('titulo').value=r.titulo||'';$('cliente').value=r.cliente||'';$('tel-cliente').value=r.telefone||'';$('validade').value=r.validade||$('validade').value;$('observacoes').value=r.observacoes||'';$('lista-mao-obra').innerHTML='';$('lista-produtos').innerHTML='';(r.maoObra||[]).forEach(i=>adicionarLinhaOrcamento('mao_obra',i));(r.produtos||[]).forEach(i=>adicionarLinhaOrcamento('produto',i));if(!coletarItens('mao_obra').length)adicionarLinhaOrcamento('mao_obra');if(!coletarItens('produto').length)adicionarLinhaOrcamento('produto')}catch(_){}}
function limparOrcamento(){if(!confirm('Limpar orçamento atual?'))return;localStorage.removeItem('fs_rascunho_orcamento');orcamentoSalvoAtualId=null;linkOrcamentoAtual='';['titulo','cliente','tel-cliente','observacoes'].forEach(id=>$(id).value='');$('lista-mao-obra').innerHTML='';$('lista-produtos').innerHTML='';adicionarLinhaOrcamento('mao_obra');adicionarLinhaOrcamento('produto');prepararDatas();gerarPreviaOrcamento();mostrarStatus('')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',iniciarGerador);else iniciarGerador();
window.abrirModalLoginGerador=abrirModalLoginGerador;window.fecharModalLoginGerador=fecharModalLoginGerador;window.abrirModalLogin=abrirModalLogin;window.fecharModalLogin=fecharModalLogin;window.adicionarLinhaOrcamento=adicionarLinhaOrcamento;window.removerLinhaOrcamento=removerLinhaOrcamento;window.gerarPreviaOrcamento=gerarPreviaOrcamento;window.baixarPDF=baixarPDF;window.salvarOrcamentoPremium=salvarOrcamentoPremium;window.enviarWhatsappPremium=enviarWhatsappPremium;window.salvarRecorrentes=salvarRecorrentes;window.usarRecorrente=usarRecorrente;window.excluirRecorrente=excluirRecorrente;window.limparOrcamento=limparOrcamento;
